import { Tenant } from '../../modules/core/tenant/entities/tenant.entity';
import { TenantDbCredentials } from '../../modules/core/tenant/tenant-credentials/entities/tenant-db-credentials.entity';

export const masterEntities = [Tenant, TenantDbCredentials];
